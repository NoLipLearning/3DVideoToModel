"""Phase 2: sparse SfM (backends/) and dense reconstruction (dense/).

Implemented in M2 (sparse) and M3 (dense). See docs/ARCHITECTURE.md
Section 4. Dense reconstruction on this project's target (Apple Silicon)
never uses COLMAP's CUDA-only patch_match_stereo -- see capability.py.
"""
